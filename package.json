{
  "name": "starter-koa2-js",
  "version": "0.0.0",
  "description": "Starter kit for server-side Node.js w/ Koa2 implementations in Docker",
  "main": "./bin/start",
  "scripts": {
    "prebuild": "del dist",
    "build": "babel src --out-dir dist",
    "coverage": "cross-env NODE_ENV=test nyc report --reporter=text-lcov > coverage.lcov && codecov",
    "dev": "nodemon -e js,json -w src -w conf -x \"npm run start:dev\" & nodemon -e js -w tst -x \"npm test\"",
    "fix": "standard --fix",
    "prerelease": "del app.tar.gz",
    "release": "cross-env NODE_ENV=production npm test && npm run build && node ./bin/compress",
    "start": "NODE_ENV=production node ./bin/start",
    "start:dev": "cross-env NODE_ENV=development npm run build && node ./bin/start",
    "pretest": "del coverage coverage.lcov .nyc_output",
    "test": "standard",
    "posttest": "cross-env NODE_ENV=test nyc --reporter=html --reporter=text mocha --recursive --exit 'tst/**/*.spec.js'",
    "version": "npm run release && git add --all",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fnalabs/starter-koa2-js.git"
  },
  "bugs": {
    "url": "https://github.com/fnalabs/starter-koa2-js/issues"
  },
  "homepage": "https://github.com/fnalabs/starter-koa2-js#readme",
  "author": "Adam Eilers",
  "license": "MIT",
  "engines": {
    "node": ">=10",
    "npm": ">=6"
  },
  "dependencies": {
    "kcors": "~2.2.2",
    "koa": "~2.7.0",
    "koa-bodyparser": "~4.2.1",
    "koa-helmet": "~4.1.0",
    "koa-logger": "~3.2.0",
    "koa-router": "~7.4.0"
  },
  "devDependencies": {
    "@babel/cli": "~7.2.3",
    "@babel/core": "~7.3.4",
    "@babel/plugin-transform-modules-commonjs": "~7.2.0",
    "@babel/register": "~7.0.0",
    "archiver": "~3.0.0",
    "babel-eslint": "~10.0.1",
    "babel-plugin-add-module-exports": "~1.0.0",
    "babel-plugin-istanbul": "~5.1.1",
    "chai": "~4.2.0",
    "chai-as-promised": "~7.1.1",
    "codecov": "~3.2.0",
    "cross-env": "~5.2.0",
    "del-cli": "~1.1.0",
    "dirty-chai": "~2.0.1",
    "mocha": "~6.0.2",
    "node-mocks-http": "~1.7.3",
    "nodemon": "~1.18.10",
    "nyc": "~13.3.0",
    "proxyquire": "~2.1.0",
    "sinon": "~7.2.7",
    "standard": "~12.0.1",
    "supertest": "~4.0.0"
  },
  "nyc": {
    "check-coverage": true,
    "per-file": true,
    "lines": 80,
    "statements": 80,
    "functions": 80,
    "branches": 80,
    "watermarks": {
      "lines": [
        80,
        90
      ],
      "functions": [
        80,
        90
      ],
      "branches": [
        80,
        90
      ],
      "statements": [
        80,
        90
      ]
    },
    "require": [
      "@babel/register"
    ],
    "sourceMap": false,
    "instrument": false
  },
  "standard": {
    "parser": "babel-eslint"
  }
}
